import * as React from "react";

export const TestPage: React.FunctionComponent = (): React.ReactElement => {
    return (
        <div className="test-page">
            <button>hi</button>
        </div>
    );
}